# Repository Guidelines

- Use JavaScript (ES6) syntax.
- Export classes or functions via `module.exports` so tests can import them.
- Place Jest tests under the `test/` directory.
- Before committing, run `npm test` to ensure all tests pass.
